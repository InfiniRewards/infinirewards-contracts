{
  "name": "infinirewards",
  "license": "UNLICENSED",
  "scripts": {
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ infinirewards",
    "create-local": "graph create --node http://localhost:8020/ infinirewards",
    "remove-local": "graph remove --node http://localhost:8020/ infinirewards",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 infinirewards",
    "test": "graph test"
  },
  "dependencies": { "@graphprotocol/graph-cli": "0.89.0" }
}
